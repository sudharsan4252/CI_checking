name: HTML CI

on:
  push:
    branches:
      - main

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Check HTML contains Hello
        run: |
          grep "Hello" index.html
          echo "Content validation passed!"